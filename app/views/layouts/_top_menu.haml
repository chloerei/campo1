#top-menu
  %ul
    %li= link_to 'Home', root_url
    - if current_logined?
      %li= link_to 'Profile', '#'
      %li= link_to 'Settings', settings_account_url
      %li.last= link_to 'Logout', logout_url
    - else
      %li= link_to 'Login', login_url
      %li.last= link_to 'Signup', signup_url
