- @page_title = t('.page_title')

= render :partial => 'settings/subheader', :locals => {:current => :password}

.section
  .section-content
    = form_for @user, :url => settings_password_path, :method => :put do |f|
      - if !f.object.errors.blank?
        = render :partial => 'share/error_messages', :locals => {:object => f.object}
      %table.form-table
        %tr
          %th= f.label :password
          %td
            = f.password_field :password, :class => 'text'
        %tr
          %th= f.label :password_confirmation
          %td
            = f.password_field :password_confirmation, :class => 'text'
        %tr
          %th= f.label :current_password
          %td
            = f.password_field :current_password, :class => 'text'
            .meta= t '.current_password_tips'
        %tr
          %th
          %td= f.submit t('.save'), :class => 'button'
