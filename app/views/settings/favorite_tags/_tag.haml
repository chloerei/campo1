%li{:id => "tag-#{CGI::escapeHTML tag}"}
  = link_to tag, tagged_topics_url(:tag => tag), :class => 'button'
  %span.actions= link_to 'X', settings_favorite_tags_url(:tags => tag), :method => :delete, :title => "remove tag '#{tag}'", :remote => true
